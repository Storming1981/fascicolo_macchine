node_modules
.next
out
build

# Variabili d'ambiente (segrete) — usare .env.example come modello
.env
.env.local
.env*.local
.env.production
.env.prod

# File caricati a runtime
uploads/*
!uploads/.gitkeep

*.tsbuildinfo
next-env.d.ts
.DS_Store

# Log di esplorazione ERP
scripts/*.log

# Dati operatori (PII) — tenere solo in locale
scripts/operators.csv

# Sync-agent ERP: segreti e artefatti (il codice è versionato, non i segreti)
sync-agent/.env
sync-agent/node_modules
sync-agent/dist
sync-agent/*.log
sync-agent/.sync.lock
